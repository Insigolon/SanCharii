import 'package:flutter/material.dart';

class PbtPassengerScreen extends StatelessWidget {
  const PbtPassengerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Destionsatoins")),
      body: const Center(child: Text("Welcome ")),
    );
  }
}
