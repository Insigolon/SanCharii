import 'package:flutter/material.dart';

class SlomScreen extends StatelessWidget {
  const SlomScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Destionsatoins")),
      body: const Center(child: Text("Welcome ")),
    );
  }
}
